record page {
    int pageNr
    string text
}

record book {
    record page frontpage
    int pages
}

func printPage(record page a, int b) void {
    print("page: " + str(a.pageNr) + "\ntext: " + a.text + "     " + str(b))
}

int a


record book d

char c = 'a'
c += 1

channel b
b << "hej"
string vari

vari << b
vari << b

func rec(int i) void {
    if (i > 0) {
        print(str(i))
        rec(i - 1)
    } else {
    }
}

rec(10)