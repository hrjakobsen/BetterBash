func range(int start, int end) int[] {
    int[] a = []
    if start < end {
        while (start < end) {
            a ::= start
            start += 1
        }
    } else {
        while (start > end) {
            a ::= start
            start -= 1
        }
    }
    return a
}


for x in range(0, 100) {
    print(intToStr(x))
}