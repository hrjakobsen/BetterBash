func range(int start, int end) int[] {
    int[] a = []
    if start < end {
        while (start < end) {
            a ::= start
            start += 1
        }
    } else {
        while (start > end) {
            a ::= start
            start -= 1
        }
    }
    return a
}

int[] a = range(0,20000)

for x in a {
    print(intToStr(x))
}
